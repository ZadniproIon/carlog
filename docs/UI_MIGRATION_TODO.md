# UI Migration TODO

- [x] Migrate all app icons from Material Icons (`Icons.*`) to Lucide icons across every screen and widget.
- [ ] Add `lucide_icons` dependency and create a single icon mapping guide so icon usage stays consistent.
- [ ] Revisit default theme mode: currently forced to `ThemeMode.light` for auth/UI review; switch back to `ThemeMode.system` before release.
